# 重聚巅峰 6V6 狂野脚本生成器（Seer-Chongju-PeakScript-exp）

一个 AI Skill，按十步工作流编排生成《赛尔号》重聚巅峰 6V6 狂野模式对战脚本（`BP.ini` / `切换.ini` / `精灵出招\*.ini`）。

本预打包包内已包含：`seer-chongju-strategy`（主技能）+ 两个子技能 `build-seerapi-encyclopedia`、`build-chongju-scripts`。

## 依赖

- 运行时需要能访问 GitHub 的网络（用于下载官方精灵数据库 `seerapi-data.sqlite`）。
- 三个技能需一并安装到同一目录，否则主技能无法调用子技能。

## 安装

把本包内三个文件夹（`seer-chongju-strategy`、`build-seerapi-encyclopedia`、`build-chongju-scripts`）复制到 `~/.codex/skills/` 即可（以下均以codex为例，实际兼容其他agent）。

## 使用

在 Codex 中调用 `$seer-chongju-strategy`，按提示依次提供：

1. 热门精灵列表（可用内置 26 年 7 月版，或自行提供 + 补充新精灵）；
2. 12 只出战精灵 + 可选玩法解析（可从 seerinfo.yuyuqaq.cn/mindmap 转文字）；
3. 战斗套装；
4. 推理强度（较高 / 默认 / 较低）；
5. 特殊要求（可选）。

技能会生成 12 只精灵出招 + BP + 切换三件套，并给出克制分析、出招设计思路、BP/切换推荐与资产说明。全程有免责声明；任务结束后会主动告知本次产生的资产。

## 可选功能

结束后可把「你已有的所有可用精灵列表」发给它，它会推荐 1~3 套 12 只阵容并附组队理由。

## 输出

- `BP.ini`、`切换.ini`、`精灵出招\*.ini`（ANSI/GBK、无 BOM、CRLF）
- 交付报告：机制解析 / 克制表 / 出招设计思路 / BP切换推荐 / 技能配置

## 数据与来源声明

- SeerAPI api-data（MIT）
- 重聚巅峰脚本开发者手册
- 属性克制表（9game.cn）
- 白龙版本狂野出招开源样本
- 特别感谢脚本大佬「极品白袜壮熊」的开源支持

仅供个人学习与娱乐，真机效果以游戏内加载为准；使用存在被游戏反作弊或账号政策处理的风险，风险自担。
